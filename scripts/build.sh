#!/bin/bash
# Build the project into a single HTML file
echo "📦 Building project into a standalone HTML file..."
npm run build
echo "✅ Build complete! You can find the single HTML file in the dist/ directory."
